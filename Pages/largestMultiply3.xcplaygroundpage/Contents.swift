//: [Previous](@previous)

import Foundation



//Given an array of at least three integers, return the largest product you can get by multiplying three numbers together.

func multipleThree(arr: [Int]) -> Int {
    let sortedArr = arr.sorted()
   
    if sortedArr[sortedArr.count - 1] > 0 && sortedArr[sortedArr.count - 2] > 0 && sortedArr[sortedArr.count - 3] > 0 {
        
    }
    //-3, -2, 0, 1, 2
return 0
}
